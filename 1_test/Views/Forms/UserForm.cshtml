@model _1_test.Models.UserWithPassword
@{
    ViewData["Title"] = "Obrazec uporabnik";
}

<div class="card-panel">
    <h1>Obrazec uporabnik (EditorForModel)</h1>
    <p class="form-note">Po kliku na Naprej se prikaze predogled (PRG).</p>

    <form asp-action="UserForm" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        @Html.EditorForModel()

        <div class="form-section">
            <button type="submit" class="btn btn-primary">Naprej</button>
        </div>
    </form>
</div>
